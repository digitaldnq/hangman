/*
package org.example;

import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class LetterPool {
    public static void main(String[] args) {
        // useLetter();
        useLetters(String.valueOf(letterPoolNew));
        System.out.println(letterPoolNew.getFirst());
    }


    static ArrayList<String> letterPool = new ArrayList<>(List.of("А", "Б", "В", "Г", "Д", "Е",
            "Ё", "Ж", "З", "И", "Й", "К", "Л", "М", "Н", "О", "П", "Р", "С", "Т", "У", "Ф", "Х", "Ц", "Ч", "Ш",
            "Щ", "Ъ", "Ы", "Ь", "Э", "Ю", "Я"));

    static ArrayList<String> letterPoolNew = new ArrayList<>(32);

    */
/*public static void useLetter() {
        System.out.println("Напишите букву: ");
        Scanner scanner = new Scanner(System.in);
        String letter = scanner.nextLine();
        if (letterPool.toString().equalsIgnoreCase(letter)) {
            letterPool.toString().equalsIgnoreCase(letter);
        }
    }*//*


    public static ArrayList<String> useLetters(String letter){

        Scanner scanner = new Scanner(System.in);
        String str = scanner.nextLine();
        letterPoolNew.add(str);

        return letterPoolNew;
    }
}
*/
