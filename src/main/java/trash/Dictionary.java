package trash;


public class Dictionary {
    public static void main(String[] args) /*throws FileNotFoundException */ {
        /*fillTheDictionary(); // метод заполнить словарь, он не сохраняет локально данные в нем, поэтому либо нужно БД либо сериализацию
        //dictionary.clear(); // полная очистка
        for (Map.Entry<Integer, String> entry : dictionary.entrySet()) {
            System.out.println(entry.getKey() + " : " + entry.getValue());
        }*/
        /*generateWord();
*/

    }

    /*static File file = new File("C:/Users/denqc/OneDrive/Desktop/Dictionary.txt");
    static Map<Integer, String> dictionary = new HashMap<>();*/

    /*public static void fillTheDictionary() throws FileNotFoundException {
        Scanner scanner = new Scanner(file);
        int i = 0;
        while (scanner.hasNextLine()) {
            dictionary.put(i, scanner.nextLine());
            i++;
        }
        scanner.close();
    }*/

    /*public static void generateWord() {
        int digit = (int) (Math.random() * 1000);
        dictionary.get(digit);
        System.out.println(dictionary.get(digit));
    }*/

}
